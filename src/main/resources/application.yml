spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/speaking
    username: "root"
    password: "password"

  jpa:
    hibernate:
      ddl-auto: create # ?? ddl-auto ?? ??? ?? ??
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        use_sql_comments: true
